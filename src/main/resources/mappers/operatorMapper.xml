<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bomul_backend.operator.model.Dao.OperatorDao">

    <insert id="insertOperator" parameterType="Operator" useGeneratedKeys="true" keyProperty="operatorId">
        INSERT INTO operator (nickname, email, password, created_at)
        VALUES ( #{nickname}, #{email}, #{password.password}, #{createdAt})
    </insert>

    <insert id="insertSalt" parameterType="Map">
        INSERT INTO salt
        VALUES (#{operatorId}, #{salt})
    </insert>

    <select id="selectSalt" parameterType="String" resultType="String">
        SELECT salt.salt
        FROM salt
        JOIN operator ON salt.operator_id=operator.operator_id
        WHERE email = #{email}
    </select>

    <select id="selectOperator" parameterType="Map" resultType="Operator">
        SELECT operator_id, nickname
        FROM operator
        WHERE email = #{email} AND password = #{password}
    </select>

</mapper>
